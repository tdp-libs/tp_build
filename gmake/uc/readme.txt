This directory contains the build files that are used for the microcontroller (uc) builds. This
creates an environment that is similar to the Arduino build but the project structure is similar to
other tdp builds.
